<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quick.boot.modules.system.mapper.SysRolesMapper">

    <select id="batchUserProjectRoleList" resultType="com.quick.boot.modules.system.vo.SysRolesVO">
        SELECT
        r.id,
        ur.user_id,
        r.role_name,
        r.role_description,
        r.create_time
        FROM
        sys_roles r
        JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE
        r.del_flag = '0'
        AND ur.del_flag = '0'
        AND r.project_id = #{projectId}
        AND ur.user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        ORDER BY ur.user_id,r.id
    </select>
</mapper>
